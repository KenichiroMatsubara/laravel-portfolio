import React, { FC } from 'react'
import FESidebar from '../components/FESidebar'

const FESearch: FC = () => {
    return (
        <div className='flex'>
            <FESidebar />
        </div>
    )
}

export default FESearch
